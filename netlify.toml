[build]
  command = "npm run build"
  publish = "dist/sofasa-dashboard/browser"

[[plugins]]
  package = "@netlify/angular-runtime"

# SPA Fallback: Redirigir todo al index.html para que Angular maneje las rutas
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
